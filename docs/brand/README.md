# Brand — Office Nizom

Not served by the app (outside `public/`) — the originals, for store listings and later use.

| File | What | Use |
|---|---|---|
| `office-nizom-logo-source.png` | The original from the owner, 1254×1254, transparent | source of everything else |
| `office-nizom-logo-800.png` | Mark + "OFFICE NIZOM", trimmed, transparent | light backgrounds only (the wordmark is dark navy) |
| `app-icon-1024.png` | The mark on white, square | Meta App Review → App icon (1024×1024) |

Served from `public/brand/` (generated from the source, mark only — the wordmark is set as
live text so it follows the light/dark theme):
`mark.png` (256, transparent — always shown on a white tile, its dark-navy half vanishes on
dark backgrounds), `icon-32.png`, `icon-192.png`, `apple-touch-icon.png` (on white), and
`/favicon.ico` (the 32px icon).
